// mitra_mobile/constants/theme.js
import theme from '../theme';

// Provide named exports expected by components
export const colors = theme.colors;
export const fonts = theme.fonts;

// Also export default theme to be compatible if needed
export default theme;
